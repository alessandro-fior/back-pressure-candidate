backpressure {
  service-name = "decoder-worker"
  http.port = 8082

  decoder {
    default-scenario-id = "phase-1b-default"
    worker-queue-capacity = 8
    worker-parallelism = 2
    base-decode-latency-ms = 200
    latency-jitter-ms = 120
    recent-results-limit = 32
  }
}
